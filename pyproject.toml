[project]
name = "fast-api-learn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nguyen Khanh Son", email = "ksondeptrai2k5@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "psycopg[binary]>=3.3.6",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
fast-api-learn = "fast_api_learn:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
